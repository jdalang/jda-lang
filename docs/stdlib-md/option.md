# option

Option Type (nullable values)

## Functions

| Function | Description |
|----------|-------------|
| `_opt_cstr_len` | Internal: compute c-string length. |
| `_opt_write_err` | Internal: write to stderr. |
| `_opt_exit` | Internal: exit with code 1. |
| `_opt_panic` | Internal: panic with message (write to stderr + exit). |
| `some` | Create a Some value. |
| `none` | Create a None value. |
| `opt_is_some` | Check if option is Some. |
| `opt_is_none` | Check if option is None. |
| `opt_unwrap` | Get the value. Panics if None. |
| `opt_expect` | Get the value, or panic with a custom message if None. |
| `opt_unwrap_or` | Get the value, or return default if None. |
| `opt_or` | Return o if Some, otherwise return other. |
| `opt_eq` | Structural equality: both None, or both Some with same value. |
| `opt_print` | Print as Some(v) or None. |
| `opt_println` | Print + newline. |

### Details

#### `_opt_cstr_len`

```jda
fn _opt_cstr_len(s: &i8) -> i64
```

Internal: compute c-string length.

#### `_opt_write_err`

```jda
fn _opt_write_err(s: &i8, len: i64)
```

Internal: write to stderr.

#### `_opt_exit`

```jda
fn _opt_exit()
```

Internal: exit with code 1.

#### `_opt_panic`

```jda
fn _opt_panic(msg: &i8)
```

Internal: panic with message (write to stderr + exit).

#### `some`

```jda
fn some(val: i64) -> &i64
```

Create a Some value.

#### `none`

```jda
fn none() -> &i64
```

Create a None value.

#### `opt_is_some`

```jda
fn opt_is_some(o: &i64) -> i64
```

Check if option is Some.

#### `opt_is_none`

```jda
fn opt_is_none(o: &i64) -> i64
```

Check if option is None.

#### `opt_unwrap`

```jda
fn opt_unwrap(o: &i64) -> i64
```

Get the value. Panics if None.

#### `opt_expect`

```jda
fn opt_expect(o: &i64, msg: &i8) -> i64
```

Get the value, or panic with a custom message if None.

#### `opt_unwrap_or`

```jda
fn opt_unwrap_or(o: &i64, def: i64) -> i64
```

Get the value, or return default if None.

#### `opt_or`

```jda
fn opt_or(o: &i64, other: &i64) -> &i64
```

Return o if Some, otherwise return other.

#### `opt_eq`

```jda
fn opt_eq(a: &i64, b: &i64) -> i64
```

Structural equality: both None, or both Some with same value.

#### `opt_print`

```jda
fn opt_print(o: &i64)
```

Print as Some(v) or None.

#### `opt_println`

```jda
fn opt_println(o: &i64)
```

Print + newline.

---

*Generated by `jda-doc-md`*
