# smartptr

Smart Pointers (Box, Rc, Arc)

## Functions

| Function | Description |
|----------|-------------|
| `box_new` | Create a boxed value (unique ownership). |
| `box_get` | Read boxed value. |
| `box_set` | Write boxed value. |
| `box_into` | Consume box, return inner value. |
| `rc_new` | Create a reference-counted value (refcount=1). |
| `rc_clone` | Clone: increment refcount, return same pointer. |
| `rc_get` | Read value. |
| `rc_set` | Write value. |
| `rc_count` | Get current reference count. |
| `rc_drop` | Decrement refcount. Returns 1 if count reached 0 (freed). |
| `weak_new` | Create a weak reference to an Rc. |
| `weak_upgrade` | Upgrade weak ref to Rc. Returns 0 if dead. |
| `weak_is_alive` | Check if weak ref target is still alive. |
| `weak_invalidate` | Mark weak reference as dead. |

### Details

#### `box_new`

```jda
fn box_new(val: i64) -> &i64
```

Create a boxed value (unique ownership).

#### `box_get`

```jda
fn box_get(b: &i64) -> i64
```

Read boxed value.

#### `box_set`

```jda
fn box_set(b: &i64, val: i64)
```

Write boxed value.

#### `box_into`

```jda
fn box_into(b: &i64) -> i64
```

Consume box, return inner value.

#### `rc_new`

```jda
fn rc_new(val: i64) -> &i64
```

Create a reference-counted value (refcount=1).

#### `rc_clone`

```jda
fn rc_clone(r: &i64) -> &i64
```

Clone: increment refcount, return same pointer.

#### `rc_get`

```jda
fn rc_get(r: &i64) -> i64
```

Read value.

#### `rc_set`

```jda
fn rc_set(r: &i64, val: i64)
```

Write value.

#### `rc_count`

```jda
fn rc_count(r: &i64) -> i64
```

Get current reference count.

#### `rc_drop`

```jda
fn rc_drop(r: &i64) -> i64
```

Decrement refcount. Returns 1 if count reached 0 (freed).

#### `weak_new`

```jda
fn weak_new(r: &i64) -> &i64
```

Create a weak reference to an Rc.

#### `weak_upgrade`

```jda
fn weak_upgrade(w: &i64) -> &i64
```

Upgrade weak ref to Rc. Returns 0 if dead.

#### `weak_is_alive`

```jda
fn weak_is_alive(w: &i64) -> i64
```

Check if weak ref target is still alive.

#### `weak_invalidate`

```jda
fn weak_invalidate(w: &i64)
```

Mark weak reference as dead.

---

*Generated by `jda-doc-md`*
