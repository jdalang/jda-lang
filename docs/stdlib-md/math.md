# math

Integer Math, Random Numbers, and Constants

## Functions

| Function | Description |
|----------|-------------|
| `math_abs` | Absolute value. |
| `math_min` | Minimum of two values. |
| `math_max` | Maximum of two values. |
| `math_clamp` | Clamp x to [lo, hi]. |
| `math_gcd` | Greatest common divisor (Euclidean algorithm). |
| `math_lcm` | Least common multiple. |
| `math_pow` | Integer exponentiation. |
| `math_isqrt` | Integer square root (floor). |
| `math_log2` | Floor of log base 2. |
| `math_factorial` | Factorial (iterative). |
| `math_fib` | Fibonacci number (iterative). |
| `math_is_prime` | Primality test (trial division). |
| `math_next_pow2` | Smallest power of 2 >= n. |
| `math_divmod` | Quotient and remainder. |
| `math_rand_seed` | Seed the random number generator. Uses a global variable via a page of memory. |
| `math_rand` | Generate pseudo-random i64. xorshift64 algorithm. |
| `math_rand_range` | Random number in range [lo, hi). |

### Details

#### `math_abs`

```jda
fn math_abs(x: i64) -> i64
```

Absolute value.

#### `math_min`

```jda
fn math_min(a: i64, b: i64) -> i64
```

Minimum of two values.

#### `math_max`

```jda
fn math_max(a: i64, b: i64) -> i64
```

Maximum of two values.

#### `math_clamp`

```jda
fn math_clamp(x: i64, lo: i64, hi: i64) -> i64
```

Clamp x to [lo, hi].

#### `math_gcd`

```jda
fn math_gcd(a: i64, b: i64) -> i64
```

Greatest common divisor (Euclidean algorithm).

#### `math_lcm`

```jda
fn math_lcm(a: i64, b: i64) -> i64
```

Least common multiple.

#### `math_pow`

```jda
fn math_pow(base: i64, exp: i64) -> i64
```

Integer exponentiation.

#### `math_isqrt`

```jda
fn math_isqrt(n: i64) -> i64
```

Integer square root (floor).

#### `math_log2`

```jda
fn math_log2(n: i64) -> i64
```

Floor of log base 2.

#### `math_factorial`

```jda
fn math_factorial(n: i64) -> i64
```

Factorial (iterative).

#### `math_fib`

```jda
fn math_fib(n: i64) -> i64
```

Fibonacci number (iterative).

#### `math_is_prime`

```jda
fn math_is_prime(n: i64) -> i64
```

Primality test (trial division).

#### `math_next_pow2`

```jda
fn math_next_pow2(n: i64) -> i64
```

Smallest power of 2 >= n.

#### `math_divmod`

```jda
fn math_divmod(a: i64, b: i64, quot_ptr: &i64, rem_ptr: &i64) -> i64
```

Quotient and remainder.

#### `math_rand_seed`

```jda
fn math_rand_seed(seed: i64) -> i64
```

Seed the random number generator.
Uses a global variable via a page of memory.

#### `math_rand`

```jda
fn math_rand() -> i64
```

Generate pseudo-random i64.
xorshift64 algorithm.

#### `math_rand_range`

```jda
fn math_rand_range(lo: i64, hi: i64) -> i64
```

Random number in range [lo, hi).

---

*Generated by `jda-doc-md`*
