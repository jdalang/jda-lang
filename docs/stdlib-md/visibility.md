# visibility

Visibility Control (pub/private convention)

## Functions

| Function | Description |
|----------|-------------|
| `_vis_cstr_len` | Internal: c-string length. |
| `_vis_write` | Internal: write to stdout. |
| `_vis_print_str` | Internal: print c-string to stdout. |
| `vis_is_public` | Check if a name is public (no leading underscore). |
| `vis_is_private` | Check if a name is private (starts with _). |
| `vis_is_internal` | Check if a name is internal (starts with __). |
| `vis_check_access` | Get access level: 0=public, 1=private, 2=internal. |
| `vis_print_access` | Print visibility level for a name. |

### Details

#### `_vis_cstr_len`

```jda
fn _vis_cstr_len(s: &i8) -> i64
```

Internal: c-string length.

#### `_vis_write`

```jda
fn _vis_write(s: &i8, len: i64)
```

Internal: write to stdout.

#### `_vis_print_str`

```jda
fn _vis_print_str(s: &i8)
```

Internal: print c-string to stdout.

#### `vis_is_public`

```jda
fn vis_is_public(name: &i8) -> i64
```

Check if a name is public (no leading underscore).

#### `vis_is_private`

```jda
fn vis_is_private(name: &i8) -> i64
```

Check if a name is private (starts with _).

#### `vis_is_internal`

```jda
fn vis_is_internal(name: &i8) -> i64
```

Check if a name is internal (starts with __).

#### `vis_check_access`

```jda
fn vis_check_access(name: &i8) -> i64
```

Get access level: 0=public, 1=private, 2=internal.

#### `vis_print_access`

```jda
fn vis_print_access(name: &i8)
```

Print visibility level for a name.

---

*Generated by `jda-doc-md`*
