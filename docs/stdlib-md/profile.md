# profile

Function-Level Profiler

## Functions

| Function | Description |
|----------|-------------|
| `_prof_cstr_len` |  |
| `_prof_write` |  |
| `_prof_print_str` |  |
| `_prof_now_ns` |  |
| `_prof_print_int` |  |
| `_prof_print_int_padded` |  |
| `prof_init` | Initialize the profiler. Call once at program start. |
| `prof_enable` | Enable profiling. |
| `prof_disable` | Disable profiling (enter/leave become no-ops). |
| `prof_enabled` | Check if profiling is enabled. |
| `prof_enter` | Start timing a function at slot (0-63). Registers name on first call. |
| `prof_leave` | Stop timing slot. Accumulate elapsed time and increment call count. |
| `prof_calls` | Get call count for slot. |
| `prof_total_ns` | Get total nanoseconds for slot. |
| `prof_avg_ns` | Get average nanoseconds per call for slot. |
| `prof_name` | Get name for slot. |
| `prof_slot_count` | Get number of active slots. |
| `prof_reset` | Reset a single slot. |
| `prof_reset_all` | Reset all slots. |
| `_prof_report_slot` |  |
| `prof_report` | Print flat profile report. |

### Details

#### `_prof_cstr_len`

```jda
fn _prof_cstr_len(s: &i8) -> i64
```

#### `_prof_write`

```jda
fn _prof_write(s: &i8, len: i64)
```

#### `_prof_print_str`

```jda
fn _prof_print_str(s: &i8)
```

#### `_prof_now_ns`

```jda
fn _prof_now_ns() -> i64
```

#### `_prof_print_int`

```jda
fn _prof_print_int(val: i64)
```

#### `_prof_print_int_padded`

```jda
fn _prof_print_int_padded(val: i64, width: i64)
```

#### `prof_init`

```jda
fn prof_init()
```

Initialize the profiler. Call once at program start.

#### `prof_enable`

```jda
fn prof_enable()
```

Enable profiling.

#### `prof_disable`

```jda
fn prof_disable()
```

Disable profiling (enter/leave become no-ops).

#### `prof_enabled`

```jda
fn prof_enabled() -> i64
```

Check if profiling is enabled.

#### `prof_enter`

```jda
fn prof_enter(slot: i64, name: &i8)
```

Start timing a function at slot (0-63). Registers name on first call.

#### `prof_leave`

```jda
fn prof_leave(slot: i64)
```

Stop timing slot. Accumulate elapsed time and increment call count.

#### `prof_calls`

```jda
fn prof_calls(slot: i64) -> i64
```

Get call count for slot.

#### `prof_total_ns`

```jda
fn prof_total_ns(slot: i64) -> i64
```

Get total nanoseconds for slot.

#### `prof_avg_ns`

```jda
fn prof_avg_ns(slot: i64) -> i64
```

Get average nanoseconds per call for slot.

#### `prof_name`

```jda
fn prof_name(slot: i64) -> &i8
```

Get name for slot.

#### `prof_slot_count`

```jda
fn prof_slot_count() -> i64
```

Get number of active slots.

#### `prof_reset`

```jda
fn prof_reset(slot: i64)
```

Reset a single slot.

#### `prof_reset_all`

```jda
fn prof_reset_all()
```

Reset all slots.

#### `_prof_report_slot`

```jda
fn _prof_report_slot(slot: i64)
```

#### `prof_report`

```jda
fn prof_report()
```

Print flat profile report.

---

*Generated by `jda-doc-md`*
