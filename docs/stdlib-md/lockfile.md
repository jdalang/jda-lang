# lockfile

Lock File for Dependency Pinning

## Functions

| Function | Description |
|----------|-------------|
| `_lock_cstr_len` | Internal: c-string length. |
| `_lock_cstr_eq` | Internal: c-string equality. |
| `_lock_write` | Internal: write to stdout. |
| `_lock_print_str` | Internal: print c-string to stdout. |
| `_lock_find` | Internal: find entry by name, -1 if not found. |
| `lock_init` | Initialize the lock file state. |
| `lock_add` | Add or update an entry. |
| `lock_remove` | Remove an entry. |
| `lock_get` | Get pinned version string ("" if not found). |
| `lock_has` | Check if name is in the lock file. |
| `lock_count` | Get number of locked entries. |
| `lock_set_hash` | Set integrity hash for an entry. |
| `lock_get_hash` | Get integrity hash (0 if not set or not found). |
| `lock_print` | Print all lock entries. |

### Details

#### `_lock_cstr_len`

```jda
fn _lock_cstr_len(s: &i8) -> i64
```

Internal: c-string length.

#### `_lock_cstr_eq`

```jda
fn _lock_cstr_eq(a: &i8, b: &i8) -> i64
```

Internal: c-string equality.

#### `_lock_write`

```jda
fn _lock_write(s: &i8, len: i64)
```

Internal: write to stdout.

#### `_lock_print_str`

```jda
fn _lock_print_str(s: &i8)
```

Internal: print c-string to stdout.

#### `_lock_find`

```jda
fn _lock_find(name: &i8) -> i64
```

Internal: find entry by name, -1 if not found.

#### `lock_init`

```jda
fn lock_init()
```

Initialize the lock file state.

#### `lock_add`

```jda
fn lock_add(name: &i8, version: &i8)
```

Add or update an entry.

#### `lock_remove`

```jda
fn lock_remove(name: &i8)
```

Remove an entry.

#### `lock_get`

```jda
fn lock_get(name: &i8) -> &i8
```

Get pinned version string ("" if not found).

#### `lock_has`

```jda
fn lock_has(name: &i8) -> i64
```

Check if name is in the lock file.

#### `lock_count`

```jda
fn lock_count() -> i64
```

Get number of locked entries.

#### `lock_set_hash`

```jda
fn lock_set_hash(name: &i8, h: i64)
```

Set integrity hash for an entry.

#### `lock_get_hash`

```jda
fn lock_get_hash(name: &i8) -> i64
```

Get integrity hash (0 if not set or not found).

#### `lock_print`

```jda
fn lock_print()
```

Print all lock entries.

---

*Generated by `jda-doc-md`*
