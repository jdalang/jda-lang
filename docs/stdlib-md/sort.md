# sort

Sorting Algorithms for Vec

## Functions

| Function | Description |
|----------|-------------|
| `sort_swap` | Internal: swap two elements in a vec's data array. |
| `sort_insertion` | Internal: insertion sort for small partitions. |
| `sort_partition` | Internal: partition for quicksort (Lomuto scheme). |
| `sort_qs` | Internal: quicksort with insertion sort for small ranges. |
| `sort_vec` | Sort a vec ascending in-place. |
| `sort_vec_desc` | Sort a vec descending in-place. |
| `sort_is_sorted` | Check if vec is sorted ascending. |
| `sort_reverse` | Reverse a vec in-place. |
| `sort_min` | Minimum element (-1 if empty). |
| `sort_max` | Maximum element (-1 if empty). |
| `sort_binary_search` | Binary search on a sorted vec. Returns index or -1. |
| `sort_unique` | Remove duplicates from a sorted vec. Returns new vec. |
| `sort_merge` | Merge two sorted vecs into one sorted vec. |

### Details

#### `sort_swap`

```jda
fn sort_swap(data: &i64, i: i64, j: i64)
```

Internal: swap two elements in a vec's data array.

#### `sort_insertion`

```jda
fn sort_insertion(data: &i64, lo: i64, hi: i64)
```

Internal: insertion sort for small partitions.

#### `sort_partition`

```jda
fn sort_partition(data: &i64, lo: i64, hi: i64) -> i64
```

Internal: partition for quicksort (Lomuto scheme).

#### `sort_qs`

```jda
fn sort_qs(data: &i64, lo: i64, hi: i64)
```

Internal: quicksort with insertion sort for small ranges.

#### `sort_vec`

```jda
fn sort_vec(v: &i64)
```

Sort a vec ascending in-place.

#### `sort_vec_desc`

```jda
fn sort_vec_desc(v: &i64)
```

Sort a vec descending in-place.

#### `sort_is_sorted`

```jda
fn sort_is_sorted(v: &i64) -> i64
```

Check if vec is sorted ascending.

#### `sort_reverse`

```jda
fn sort_reverse(v: &i64)
```

Reverse a vec in-place.

#### `sort_min`

```jda
fn sort_min(v: &i64) -> i64
```

Minimum element (-1 if empty).

#### `sort_max`

```jda
fn sort_max(v: &i64) -> i64
```

Maximum element (-1 if empty).

#### `sort_binary_search`

```jda
fn sort_binary_search(v: &i64, val: i64) -> i64
```

Binary search on a sorted vec. Returns index or -1.

#### `sort_unique`

```jda
fn sort_unique(v: &i64) -> &i64
```

Remove duplicates from a sorted vec. Returns new vec.

#### `sort_merge`

```jda
fn sort_merge(a: &i64, b: &i64) -> &i64
```

Merge two sorted vecs into one sorted vec.

---

*Generated by `jda-doc-md`*
