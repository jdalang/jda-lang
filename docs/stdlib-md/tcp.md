# tcp

; jda::net::tcp — TCP Socket API

## Constants

```jda
SYS_SOCKET     = 41
```

```jda
SYS_BIND       = 49
```

```jda
SYS_LISTEN     = 50
```

```jda
SYS_ACCEPT4    = 288
```

```jda
SYS_CONNECT    = 42
```

```jda
SYS_SENDTO     = 44
```

```jda
SYS_RECVFROM   = 45
```

```jda
SYS_CLOSE      = 3
```

```jda
SYS_SETSOCKOPT = 54
```

```jda
SYS_FCNTL      = 72
```

```jda
AF_INET      = 2
```

```jda
SOCK_STREAM  = 1
```

```jda
SOCK_NONBLOCK = 2048    ; create non-blocking socket directly
```

```jda
SOL_SOCKET   = 1
```

```jda
SO_REUSEADDR = 2
```

```jda
SO_REUSEPORT = 15
```

```jda
IPPROTO_TCP  = 6
```

```jda
TCP_NODELAY  = 1
```

```jda
F_SETFL      = 4
```

```jda
O_NONBLOCK   = 2048
```

```jda
EAGAIN = 11
```

## Structs

### `SockAddrIn`

```jda
struct SockAddrIn {
    sin_family: u16     ; AF_INET = 2
    sin_port:   u16     ; port in network byte order (big-endian)
    sin_addr:   u32     ; IPv4 address in network byte order
    sin_zero:   [8]u8   ; padding
}
```

### `TcpStream`

```jda
struct TcpStream {
    fd:        i32
    peer_addr: u32
    peer_port: u16
}
```

### `TcpListener`

```jda
struct TcpListener {
    fd:        i32
    local_port: u16
}
```

## Functions

| Function | Description |
|----------|-------------|
| `tcp_close_fd` |  |
| `ip_to_u32` |  |
| `htons` |  |
| `ntohs` |  |
| `htonl` |  |
| `ntohl` |  |

### Details

#### `tcp_close_fd`

```jda
fn tcp_close_fd(fd: i32)
```

#### `ip_to_u32`

```jda
fn ip_to_u32(s: ref []i8) -> u32
```

#### `htons`

```jda
fn htons(x: u16) -> u16 => ((x & 0xFF) << 8) | (x >> 8)
```

#### `ntohs`

```jda
fn ntohs(x: u16) -> u16 => htons(x)
```

#### `htonl`

```jda
fn htonl(x: u32) -> u32 => ((x & 0xFF) << 24) | (((x>>8) & 0xFF) << 16) | (((x>>16) & 0xFF) << 8) | (x >> 24)
```

#### `ntohl`

```jda
fn ntohl(x: u32) -> u32 => htonl(x)
```

---

*Generated by `jda-doc-md`*
