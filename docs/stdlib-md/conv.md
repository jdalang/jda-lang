# conv

Value Conversions (itoa, atoi, hex)

## Functions

| Function | Description |
|----------|-------------|
| `conv_itoa` | Integer to decimal string. Returns length written. |
| `conv_atoi` | Decimal string to integer. |
| `conv_itohex` | Integer to hex string (lowercase). Returns length. |
| `conv_hextoi` | Hex string to integer. |
| `conv_itobin` | Integer to binary string. Returns length. |
| `conv_itoa_pad` | Integer to zero-padded decimal string. Returns width. |

### Details

#### `conv_itoa`

```jda
fn conv_itoa(val: i64, dst: &i8) -> i64
```

Integer to decimal string. Returns length written.

#### `conv_atoi`

```jda
fn conv_atoi(src: &i8, len: i64) -> i64
```

Decimal string to integer.

#### `conv_itohex`

```jda
fn conv_itohex(val: i64, dst: &i8) -> i64
```

Integer to hex string (lowercase). Returns length.

#### `conv_hextoi`

```jda
fn conv_hextoi(src: &i8, len: i64) -> i64
```

Hex string to integer.

#### `conv_itobin`

```jda
fn conv_itobin(val: i64, dst: &i8) -> i64
```

Integer to binary string. Returns length.

#### `conv_itoa_pad`

```jda
fn conv_itoa_pad(val: i64, dst: &i8, width: i64) -> i64
```

Integer to zero-padded decimal string. Returns width.

---

*Generated by `jda-doc-md`*
