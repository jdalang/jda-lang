# lint

Code Style Checker / Linter Helpers

## Functions

| Function | Description |
|----------|-------------|
| `_lint_cstr_len` |  |
| `_lint_write` |  |
| `_lint_print_str` |  |
| `_lint_byte_at` |  |
| `_lint_is_lower` |  |
| `_lint_is_upper_ch` |  |
| `_lint_is_digit` |  |
| `_lint_is_underscore` |  |
| `lint_init` | Initialize the linter. |
| `lint_reset` | Reset warning count. |
| `lint_is_snake` | Check if name is valid snake_case: lowercase, digits, underscores. First char... |
| `lint_is_upper` | Check if name is valid UPPER_SNAKE: uppercase, digits, underscores. |
| `lint_is_pascal` | Check if name is valid PascalCase: starts with uppercase, then letters/digits. |
| `lint_check_fn` | Check if valid function name: snake_case. |
| `lint_check_const` | Check if valid constant name: UPPER_SNAKE. |
| `lint_check_struct` | Check if valid struct name: PascalCase. |
| `lint_check_var` | Check if valid variable name: snake_case. |
| `lint_warn` | Print a lint warning: "[LINT] name: msg" |
| `lint_add_warning` | Add to warning count (silent). |
| `lint_warning_count` | Get total warning count. |
| `lint_check_line_len` | Check if line length is within max chars. Returns 1 if ok. |
| `lint_has_trailing_ws` | Check if line has trailing whitespace (space or tab before null). |
| `lint_summary` | Print lint summary. |

### Details

#### `_lint_cstr_len`

```jda
fn _lint_cstr_len(s: &i8) -> i64
```

#### `_lint_write`

```jda
fn _lint_write(s: &i8, len: i64)
```

#### `_lint_print_str`

```jda
fn _lint_print_str(s: &i8)
```

#### `_lint_byte_at`

```jda
fn _lint_byte_at(buf: &i8, idx: i64) -> i64
```

#### `_lint_is_lower`

```jda
fn _lint_is_lower(ch: i64) -> i64
```

#### `_lint_is_upper_ch`

```jda
fn _lint_is_upper_ch(ch: i64) -> i64
```

#### `_lint_is_digit`

```jda
fn _lint_is_digit(ch: i64) -> i64
```

#### `_lint_is_underscore`

```jda
fn _lint_is_underscore(ch: i64) -> i64
```

#### `lint_init`

```jda
fn lint_init()
```

Initialize the linter.

#### `lint_reset`

```jda
fn lint_reset()
```

Reset warning count.

#### `lint_is_snake`

```jda
fn lint_is_snake(name: &i8) -> i64
```

Check if name is valid snake_case: lowercase, digits, underscores.
First char must be lowercase or underscore.

#### `lint_is_upper`

```jda
fn lint_is_upper(name: &i8) -> i64
```

Check if name is valid UPPER_SNAKE: uppercase, digits, underscores.

#### `lint_is_pascal`

```jda
fn lint_is_pascal(name: &i8) -> i64
```

Check if name is valid PascalCase: starts with uppercase, then letters/digits.

#### `lint_check_fn`

```jda
fn lint_check_fn(name: &i8) -> i64
```

Check if valid function name: snake_case.

#### `lint_check_const`

```jda
fn lint_check_const(name: &i8) -> i64
```

Check if valid constant name: UPPER_SNAKE.

#### `lint_check_struct`

```jda
fn lint_check_struct(name: &i8) -> i64
```

Check if valid struct name: PascalCase.

#### `lint_check_var`

```jda
fn lint_check_var(name: &i8) -> i64
```

Check if valid variable name: snake_case.

#### `lint_warn`

```jda
fn lint_warn(name: &i8, msg: &i8)
```

Print a lint warning: "[LINT] name: msg"

#### `lint_add_warning`

```jda
fn lint_add_warning(msg: &i8)
```

Add to warning count (silent).

#### `lint_warning_count`

```jda
fn lint_warning_count() -> i64
```

Get total warning count.

#### `lint_check_line_len`

```jda
fn lint_check_line_len(line: &i8, maxlen: i64) -> i64
```

Check if line length is within max chars. Returns 1 if ok.

#### `lint_has_trailing_ws`

```jda
fn lint_has_trailing_ws(line: &i8) -> i64
```

Check if line has trailing whitespace (space or tab before null).

#### `lint_summary`

```jda
fn lint_summary()
```

Print lint summary.

---

*Generated by `jda-doc-md`*
