# slice

Slice / Dynamic View

## Functions

| Function | Description |
|----------|-------------|
| `slice_new` | Create a slice view. |
| `slice_from` | View of entire array (offset 0, length n). |
| `slice_len` | Get length. |
| `slice_get` | Get element at index i (relative to slice start). |
| `slice_set` | Set element at index i (mutates backing array). |
| `slice_sub` | Sub-slice [start, end) relative to this slice. |
| `slice_first` | First element. |
| `slice_last` | Last element. |
| `slice_eq` | Element-wise equality. |
| `slice_contains` | Check if value is in slice. |
| `slice_index_of` | Find first index of value, or -1. |
| `slice_sum` | Sum of elements. |
| `slice_copy_to` | Copy slice elements to a raw array. |
| `slice_print` | Print as [1, 2, 3]. |
| `slice_println` | Print + newline. |

### Details

#### `slice_new`

```jda
fn slice_new(data: &i64, off: i64, len: i64) -> &i64
```

Create a slice view.

#### `slice_from`

```jda
fn slice_from(data: &i64, n: i64) -> &i64
```

View of entire array (offset 0, length n).

#### `slice_len`

```jda
fn slice_len(s: &i64) -> i64
```

Get length.

#### `slice_get`

```jda
fn slice_get(s: &i64, i: i64) -> i64
```

Get element at index i (relative to slice start).

#### `slice_set`

```jda
fn slice_set(s: &i64, i: i64, val: i64)
```

Set element at index i (mutates backing array).

#### `slice_sub`

```jda
fn slice_sub(s: &i64, start: i64, end: i64) -> &i64
```

Sub-slice [start, end) relative to this slice.

#### `slice_first`

```jda
fn slice_first(s: &i64) -> i64
```

First element.

#### `slice_last`

```jda
fn slice_last(s: &i64) -> i64
```

Last element.

#### `slice_eq`

```jda
fn slice_eq(a: &i64, b: &i64) -> i64
```

Element-wise equality.

#### `slice_contains`

```jda
fn slice_contains(s: &i64, val: i64) -> i64
```

Check if value is in slice.

#### `slice_index_of`

```jda
fn slice_index_of(s: &i64, val: i64) -> i64
```

Find first index of value, or -1.

#### `slice_sum`

```jda
fn slice_sum(s: &i64) -> i64
```

Sum of elements.

#### `slice_copy_to`

```jda
fn slice_copy_to(s: &i64, dst: &i64)
```

Copy slice elements to a raw array.

#### `slice_print`

```jda
fn slice_print(s: &i64)
```

Print as [1, 2, 3].

#### `slice_println`

```jda
fn slice_println(s: &i64)
```

Print + newline.

---

*Generated by `jda-doc-md`*
