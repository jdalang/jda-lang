# testing

Test Framework and Assertions

## Functions

| Function | Description |
|----------|-------------|
| `test_stderr` | Internal: write to stderr. |
| `test_stderr_int` | Internal: write integer to stderr. |
| `test_begin` | Begin a named test. |
| `test_end` | End current test. |
| `test_assert_fail` | Internal: report assertion failure. |
| `assert_eq` | Assert a == b. |
| `assert_ne` | Assert a != b. |
| `assert_true` | Assert val is truthy (non-zero). |
| `assert_false` | Assert val is falsy (zero). |
| `assert_gt` | Assert a > b. |
| `assert_lt` | Assert a < b. |
| `assert_ge` | Assert a >= b. |
| `assert_le` | Assert a <= b. |
| `test_summary` | Print test summary. Returns number of failures. |

### Details

#### `test_stderr`

```jda
fn test_stderr(msg: &i8, len: i64)
```

Internal: write to stderr.

#### `test_stderr_int`

```jda
fn test_stderr_int(val: i64)
```

Internal: write integer to stderr.

#### `test_begin`

```jda
fn test_begin(name: &i8, len: i64)
```

Begin a named test.

#### `test_end`

```jda
fn test_end()
```

End current test.

#### `test_assert_fail`

```jda
fn test_assert_fail(op: &i8, oplen: i64, a: i64, b: i64)
```

Internal: report assertion failure.

#### `assert_eq`

```jda
fn assert_eq(a: i64, b: i64)
```

Assert a == b.

#### `assert_ne`

```jda
fn assert_ne(a: i64, b: i64)
```

Assert a != b.

#### `assert_true`

```jda
fn assert_true(val: i64)
```

Assert val is truthy (non-zero).

#### `assert_false`

```jda
fn assert_false(val: i64)
```

Assert val is falsy (zero).

#### `assert_gt`

```jda
fn assert_gt(a: i64, b: i64)
```

Assert a > b.

#### `assert_lt`

```jda
fn assert_lt(a: i64, b: i64)
```

Assert a < b.

#### `assert_ge`

```jda
fn assert_ge(a: i64, b: i64)
```

Assert a >= b.

#### `assert_le`

```jda
fn assert_le(a: i64, b: i64)
```

Assert a <= b.

#### `test_summary`

```jda
fn test_summary() -> i64
```

Print test summary. Returns number of failures.

---

*Generated by `jda-doc-md`*
