# csv

CSV Reader/Writer

## Functions

| Function | Description |
|----------|-------------|
| `csv_parse_row` | Parse a single CSV row from src. Returns CsvRow. |
| `csv_field_count` | Number of fields in a row. |
| `csv_field` | Copy field at index to dst. Returns length of field. |
| `csv_field_int` | Parse field at index as integer. |
| `csv_parse_all` | Parse all rows from CSV data. Returns vec of CsvRow pointers. |
| `csv_row_count` | Number of rows. |
| `csv_get_row` | Get row at index. |
| `csv_write_row` | Write a CSV row. fields is array of &i8, lengths is array of i64. Returns byt... |

### Details

#### `csv_parse_row`

```jda
fn csv_parse_row(src: &i8, len: i64) -> &i64
```

Parse a single CSV row from src. Returns CsvRow.

#### `csv_field_count`

```jda
fn csv_field_count(row: &i64) -> i64
```

Number of fields in a row.

#### `csv_field`

```jda
fn csv_field(row: &i64, idx: i64, dst: &i8) -> i64
```

Copy field at index to dst. Returns length of field.

#### `csv_field_int`

```jda
fn csv_field_int(row: &i64, idx: i64) -> i64
```

Parse field at index as integer.

#### `csv_parse_all`

```jda
fn csv_parse_all(src: &i8, len: i64) -> &i64
```

Parse all rows from CSV data. Returns vec of CsvRow pointers.

#### `csv_row_count`

```jda
fn csv_row_count(rows: &i64) -> i64
```

Number of rows.

#### `csv_get_row`

```jda
fn csv_get_row(rows: &i64, idx: i64) -> &i64
```

Get row at index.

#### `csv_write_row`

```jda
fn csv_write_row(fields: &i64, lengths: &i64, count: i64, dst: &i8) -> i64
```

Write a CSV row. fields is array of &i8, lengths is array of i64.
Returns bytes written to dst.

---

*Generated by `jda-doc-md`*
