# sync

WaitGroup and Barrier Synchronization

## Functions

| Function | Description |
|----------|-------------|
| `_sync_futex_wait` | Internal: futex wait. |
| `_sync_futex_wake_all` | Internal: futex wake all. |
| `wg_new` | Create a new WaitGroup with counter=0. |
| `wg_add` | Add n to the WaitGroup counter. |
| `wg_done` | Decrement the counter. Wakes waiters when it reaches 0. |
| `wg_count` | Get current counter value. |
| `wg_wait` | Block until counter reaches 0. |
| `wg_reset` | Reset counter to 0. |
| `barrier_new` | Create a barrier for n tasks. |
| `barrier_wait` | Wait at barrier. Returns 1 for the last arrival (leader), 0 otherwise. |
| `barrier_arrived` | Get number of tasks that have arrived. |
| `barrier_total` | Get total expected tasks. |
| `barrier_reset` | Reset barrier for n tasks. |
| `once_new` | Create a new once flag. |
| `once_do` | Try to claim the once. Returns 1 if caller should execute, 0 if already done. |
| `once_is_done` | Check if already executed. |
| `once_reset` | Reset to not-done. |

### Details

#### `_sync_futex_wait`

```jda
fn _sync_futex_wait(addr: &i64, expected: i64) -> i64
```

Internal: futex wait.

#### `_sync_futex_wake_all`

```jda
fn _sync_futex_wake_all(addr: &i64) -> i64
```

Internal: futex wake all.

#### `wg_new`

```jda
fn wg_new() -> &i64
```

Create a new WaitGroup with counter=0.

#### `wg_add`

```jda
fn wg_add(wg: &i64, n: i64)
```

Add n to the WaitGroup counter.

#### `wg_done`

```jda
fn wg_done(wg: &i64)
```

Decrement the counter. Wakes waiters when it reaches 0.

#### `wg_count`

```jda
fn wg_count(wg: &i64) -> i64
```

Get current counter value.

#### `wg_wait`

```jda
fn wg_wait(wg: &i64)
```

Block until counter reaches 0.

#### `wg_reset`

```jda
fn wg_reset(wg: &i64)
```

Reset counter to 0.

#### `barrier_new`

```jda
fn barrier_new(n: i64) -> &i64
```

Create a barrier for n tasks.

#### `barrier_wait`

```jda
fn barrier_wait(b: &i64) -> i64
```

Wait at barrier. Returns 1 for the last arrival (leader), 0 otherwise.

#### `barrier_arrived`

```jda
fn barrier_arrived(b: &i64) -> i64
```

Get number of tasks that have arrived.

#### `barrier_total`

```jda
fn barrier_total(b: &i64) -> i64
```

Get total expected tasks.

#### `barrier_reset`

```jda
fn barrier_reset(b: &i64, n: i64)
```

Reset barrier for n tasks.

#### `once_new`

```jda
fn once_new() -> &i64
```

Create a new once flag.

#### `once_do`

```jda
fn once_do(f: &i64) -> i64
```

Try to claim the once. Returns 1 if caller should execute, 0 if already done.

#### `once_is_done`

```jda
fn once_is_done(f: &i64) -> i64
```

Check if already executed.

#### `once_reset`

```jda
fn once_reset(f: &i64)
```

Reset to not-done.

---

*Generated by `jda-doc-md`*
