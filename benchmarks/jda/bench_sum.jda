fn bench_sum_loop(n: i64) {
    let iter = 0
    loop iter < n {
        let s = 0
        let i = 0
        loop i < 1000 {
            s = s + i
            i = i + 1
        }
        iter = iter + 1
    }
}
