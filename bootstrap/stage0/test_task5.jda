fn set_val(p: &i64, val: i64) {
    p[0] = val
}

fn main() {
    let arr = i64[10]
    set_val(&arr[0], 42)
    if arr[0] == 42 { print("OK\n") }
    if arr[0] == 0  { print("ZERO\n") }
    if arr[0] == 42 { } else { print("OTHER\n") }
}
