# SHA-256 checksums for bootstrap binaries
# Verify: cd bootstrap/bin && shasum -a 256 -c CHECKSUMS
#
# jda1-bootstrap is the self-hosted Jda compiler (stage 3 fixed-point binary).
# It compiles jda1.jda without requiring NASM or jda0.
#
# To update: rebuild the self-host pipeline, verify convergence (stage N == stage N+1),
# then copy the converged binary here and update this checksum.

a97a2e8d419696a4cf8eb9260f2b1ad932bfd3971ef922daab8b281b453557e5  jda1-bootstrap
