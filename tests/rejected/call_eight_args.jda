// Seven is the codegen limit; an eighth argument is rejected rather than
// silently dropped.
fn eight(a: i64, b: i64, c: i64, d: i64, e: i64, f: i64, g: i64) -> i64 { ret g }

fn main() {
    print("{eight(1, 2, 3, 4, 5, 6, 7, 8)}\n")
}
