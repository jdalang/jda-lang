fn assert_eq(a: i64, b: i64) {
    if a != b {
        syscall(60, 1, 0, 0)
    }
}

fn main() -> i64 {
    assert_eq(1, 1)
    assert_eq(42, 42)
    assert_eq(0, 0)
    print "assert_eq ok"
    ret 0
}
