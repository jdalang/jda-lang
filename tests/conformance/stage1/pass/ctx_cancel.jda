// Cancel a context, check done and err code
fn main() {
    let bg = ctx_background()
    let ctx = ctx_with_cancel(bg)
    ctx_cancel(ctx)
    let d = ctx_done(ctx)
    let e = ctx_err(ctx)
    print_int(d)
    print_int(e)
}
