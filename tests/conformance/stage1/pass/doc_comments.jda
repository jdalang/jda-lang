;; A documented helper function.
fn helper(x: i64) -> i64 {
    ret x + 1
}

;; Main entry point with doc comment.
fn main() -> i64 {
    let r = helper(41)
    if r != 42 { syscall(60, 1, 0, 0) }
    print "doc_comments ok"
    ret 0
}
