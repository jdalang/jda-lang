struct Pair {
    x: i64
    y: i64
}

fn test_pair(p: &Pair) -> i64 {
    if p.x == 5 { ret 1 }
    ret 0
}

fn main() {
    let p = Pair{}
    p.x = 5
    p.y = 10
    let ok = test_pair(&p)
    if ok == 1 { print("ok") }
}
