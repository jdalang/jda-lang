fn main() {
    let m = map_new(16)
    map_put(m, 5, 100)
    let h1 = map_has(m, 5)
    print("{h1}")
    let h2 = map_has(m, 99)
    print("{h2}")
    let d = map_del(m, 5)
    print("{d}")
    let h3 = map_has(m, 5)
    print("{h3}")
}
