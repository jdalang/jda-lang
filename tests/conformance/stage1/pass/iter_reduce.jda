fn main() {
    let arr: &i64 = alloc_pages(1)
    arr[0] = 10
    arr[1] = 20
    arr[2] = 30
    let total = reduce(arr, 3, 0, fn(a: i64, x: i64) -> i64 {
        ret a + x
    })
    print("{total}")
}
