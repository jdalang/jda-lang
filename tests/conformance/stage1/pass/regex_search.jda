fn main() {
    let r1 = regex_search("world", 5, "hello world!", 12)
    print("{r1}")
    let r2 = regex_search("xyz", 3, "hello world!", 12)
    let found = 0
    if r2 >= 0 { found = 1 }
    print("{found}")
}
