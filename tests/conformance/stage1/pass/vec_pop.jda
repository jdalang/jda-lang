fn main() {
    let v = vec_new(4)
    vec_push(v, 10)
    vec_push(v, 20)
    vec_push(v, 30)
    let a = vec_pop(v)
    print("{a}")
    let b = vec_pop(v)
    print("{b}")
    let len = vec_len(v)
    print("{len}")
}
