// Test AMD GPU detection — rocm_has_amd() returns 0 or 1

fn main() -> i64 {
    let has = rocm_has_amd()
    if has == 0 {
        print "rocm check ok"
    }
    if has == 1 {
        print "rocm check ok"
    }
    ret 0
}
