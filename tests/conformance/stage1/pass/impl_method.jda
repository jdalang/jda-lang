struct Point {
    x: i64
    y: i64
}

impl Point {
    fn sum(&self) -> i64 {
        ret self.x + self.y
    }
}

fn main() {
    let p = Point{}
    p.x = 10
    p.y = 32
    let s = p.sum()
    if s == 42 {
        print("ok")
    }
}
