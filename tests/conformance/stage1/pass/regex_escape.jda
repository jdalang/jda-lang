fn main() {
    let r1 = regex_match("\\d+", 3, "42", 2)
    print("{r1}")
    let r2 = regex_match("\\w+", 3, "hello", 5)
    print("{r2}")
    let r3 = regex_match("\\s+", 3, "  ", 2)
    print("{r3}")
    let r4 = regex_match("a\\.b", 4, "a.b", 3)
    print("{r4}")
}
