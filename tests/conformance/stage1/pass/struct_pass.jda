struct Point {
    x: i64
    y: i64
}

fn sum_fields(p: &Point) -> i64 {
    ret p.x + p.y
}

fn main() {
    let p = Point{}
    p.x = 3
    p.y = 7
    let s = sum_fields(&p)
    if s == 10 { print("10") }
}
