fn main() {
    let a = arena_new(1)
    let p1 = arena_alloc(a, 8)
    p1[0] = 10
    arena_reset(a)
    let p2 = arena_alloc(a, 8)
    p2[0] = 20
    let v = p2[0]
    if v == 20 {
        print("ok")
    }
    arena_destroy(a, 1)
}
