fn main() {
    let a: i64 = -5
    let r1 = a.clamp(0, 50)
    print("{r1}")
    let b: i64 = 25
    let r2 = b.clamp(0, 50)
    print("{r2}")
    let c: i64 = 100
    let r3 = c.clamp(0, 50)
    print("{r3}")
}
