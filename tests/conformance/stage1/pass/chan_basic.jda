fn main() {
    let ch = chan_new(4)
    chan_send(ch, 42)
    chan_send(ch, 99)
    let v1 = chan_recv(ch)
    let v2 = chan_recv(ch)
    print_int(v1)
    print_int(v2)
    chan_close(ch)
    let v3 = chan_recv(ch)
    if v3 + 1 == 0 {
        print("closed")
    }
}
