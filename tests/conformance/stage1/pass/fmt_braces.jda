// Test: brace style — opening brace on same line

fn factorial(n: i64) -> i64 {
  if n <= 1 {
    ret 1
  }
  ret n * factorial(n - 1)
}

fn main() -> i64 {
  let r = factorial(5)
  if r != 120 {
    syscall(60, 1, 0, 0)
  }
  print "fmt_braces ok"
  ret 0
}
