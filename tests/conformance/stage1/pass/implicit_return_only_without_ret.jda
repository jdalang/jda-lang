// Implicit return applies only when a function has no explicit `ret`.
// A function that returns explicitly still falls through to 0.
fn f(x: i64) -> i64 {
    if x > 0 { ret 1 }
    x
}
fn main() {
    let r = f(-5)
    print("{r}\n")
}
