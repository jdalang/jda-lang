fn main() {
    let s = str_from("hello world", 11)
    let sub = str_slice(s, 6, 11)
    let expected = str_from("world", 5)
    let eq = str_eq(sub, expected)
    if eq == 1 { print("ok") }
    if eq == 0 { print("fail") }
}
