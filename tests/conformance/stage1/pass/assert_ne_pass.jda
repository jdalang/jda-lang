fn assert_ne(a: i64, b: i64) {
    if a == b {
        syscall(60, 1, 0, 0)
    }
}

fn main() -> i64 {
    assert_ne(1, 2)
    assert_ne(0, 1)
    assert_ne(42, 43)
    print "assert_ne ok"
    ret 0
}
