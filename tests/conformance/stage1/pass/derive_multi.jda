derive(Debug, Eq, Clone)
struct Color {
    r: i64
    g: i64
    b: i64
}

fn main() {
    let c1 = Color{}
    c1.r = 255
    c1.g = 128
    c1.b = 0
    let c2 = Color{}
    Color_clone(&c1, &c2)
    let eq = Color_eq(&c1, &c2)
    print("{eq}")
    Color_debug(&c1)
}
