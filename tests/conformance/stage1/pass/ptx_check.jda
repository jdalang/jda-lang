; Test GPU detection — gpu_has_nvidia() returns 0 or 1

fn main() -> i64 {
    let has = gpu_has_nvidia()
    if has == 0 {
        print "gpu check ok"
    }
    if has == 1 {
        print "gpu check ok"
    }
    ret 0
}
