fn assert_true(val: i64) {
    if val == 0 {
        syscall(60, 1, 0, 0)
    }
}

fn main() -> i64 {
    assert_true(1)
    assert_true(42)
    assert_true(100)
    print "assert_true ok"
    ret 0
}
