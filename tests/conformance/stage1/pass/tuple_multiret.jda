fn divmod(a: i64, b: i64) -> &i64 {
    let q = a / b
    let r = a - q * b
    ret pair(q, r)
}

fn main() {
    let r = divmod(17, 5)
    let q = fst(r)
    let rem = snd(r)
    print("{q}{rem}")
}
