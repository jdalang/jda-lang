fn main() -> i64 {
    let x: f64 = 3.14
    print_f64(x)
    print "\n"

    let y: f64 = 2.0
    print_f64(y)
    print "\n"

    let z: f64 = f64_abs(0.0 - 5.5)
    let zi = f64_to_int(z)
    if zi == 5 { print "abs ok\n" }

    ret 0
}
