fn main() -> i64 {
    ; Sum even numbers 0-9 using continue to skip odds
    let sum = 0
    let i = 0
    loop i < 10 {
        let cur = i
        i = i + 1
        let rem = cur % 2
        if rem != 0 { continue }
        sum = sum + cur
    }
    print_int(sum)
    print("\n")
    ret 0
}
