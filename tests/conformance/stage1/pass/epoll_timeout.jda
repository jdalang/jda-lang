// Test epoll_wait with timeout=0 on empty epoll returns 0
fn main() {
    let epfd = epoll_create1(0)
    let ev: &i64 = alloc_pages(1)
    // epoll_wait with no fds registered, timeout=0 → returns 0 immediately
    let n = epoll_wait(epfd, ev, 1, 0)
    if n == 0 {
        print("zero")
    }
}
