fn double(x: i64) -> i64 {
    ret x * 2
}

fn apply(f: i64, val: i64) -> i64 {
    let r = call_fn(f, val)
    ret r
}

fn main() {
    let fp = fn_addr(double)
    let result = apply(fp, 5)
    if result == 10 { print("10") }
}
