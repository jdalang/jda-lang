struct Point {
    x: i64
    y: i64
}

fn sum_point(p: &Point) -> i64 {
    ret p.x + p.y
}

fn set_point(p: &Point, x: i64, y: i64) {
    p.x = x
    p.y = y
}

fn main() {
    let p = Point{}
    set_point(&p, 3, 7)
    let s = sum_point(&p)
    if s == 10 { print("ok") }
}
