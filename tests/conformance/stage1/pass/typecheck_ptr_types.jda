fn write_val(buf: &i64, idx: i64, val: i64) {
    buf[idx] = val
}

fn read_val(buf: &i64, idx: i64) -> i64 {
    ret buf[idx]
}

fn main() {
    let buf = i64[4]
    write_val(buf, 0, 99)
    let v = read_val(buf, 0)
    if v == 99 { print("ok") }
}
