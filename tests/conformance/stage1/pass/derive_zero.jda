derive(Zero)
struct Data {
    x: i64
    y: i64
}

fn main() {
    let d = Data{}
    d.x = 100
    d.y = 200
    Data_zero(&d)
    let vx = d.x
    let vy = d.y
    print("{vx}")
    print("{vy}")
}
