fn main() {
    let v = vec_new(4)
    vec_push(v, 10)
    vec_push(v, 20)
    vec_push(v, 30)
    let len = vec_len(v)
    print("{len}")
    let a = vec_get(v, 0)
    print("{a}")
    let b = vec_get(v, 1)
    let c = vec_get(v, 2)
    let sum = b + c
    print("{sum}")
}
