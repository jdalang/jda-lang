// chan_recv on a closed channel returns -1
fn main() {
    let ch = chan_new(4)
    chan_send(ch, 42)
    chan_close(ch)
    let v1 = chan_recv(ch)
    let v2 = chan_recv(ch)
    if v1 == 42 { print("42") }
    if v2 + 1 == 0 { print("closed") }
}
