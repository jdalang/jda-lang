; Combined arithmetic operations
fn main() {
    let a = 10
    let b = 3
    let sum = a + b
    let diff = a - b
    let prod = a * b
    let quot = a / b
    let total = sum + diff + prod + quot
    ; 13 + 7 + 30 + 3 = 53
    if total == 53 { print("ok") }
}
